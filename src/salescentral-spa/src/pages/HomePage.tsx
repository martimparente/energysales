
export function HomePage() {
/*     const [content, loading] = useFetch("/api/")
    const { isLogin } = useAuth() 
    const user = isLogin()

    if (user) {
        return <Navigate to={"/me"} replace={true} />
    }*/

    return (
        <div>
            <h1>Home</h1>
        </div>
    )
}
