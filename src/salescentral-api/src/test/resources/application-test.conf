ktor {
    development = false
    deployment {
        port = 8080
    }
    application {
        modules = [ pt.isel.ps.salescentral.ApplicationKt.module ]
    }
}

storage {
    driverClassName = "org.postgresql.Driver"
    jdbcURL = "jdbc:postgresql://localhost:5434/testing_db"
    user = "testing_user"
    password = "testing_password"
}


jwt {
  audience = "audience"
  issuer = "issuer"
  realm = "realm"
  secret = "secret" //TODO: change this to a real secret .env
}
